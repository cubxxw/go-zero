interface:
  display_name: "Go Zero"
  short_description: "Repo-specific patterns and workflows for go-zero"
  default_prompt: "Use the go-zero repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true